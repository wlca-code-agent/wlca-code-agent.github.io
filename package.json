{
  "dependencies": {
    "@floating-ui/dom": "^1.6.11",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "scripts": {
    "dev": "npx parcel src/index.html",
    "build": "npx parcel build src/index.html --dist-dir release --no-source-maps"
  }
}
